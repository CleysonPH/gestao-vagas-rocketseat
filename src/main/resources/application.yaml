spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gestao_vagas
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        "[format_sql]": true

security:
  token:
    secret:
      company: JAVAGAS_@123
      candidate: JAVAGAS_@123CADIDATE
